# Minimal dependencies for the cloud demo image.
# DOCLENS_DEMO_MODE skips the VLM, so torch/transformers/bitsandbytes aren't needed.
# Real VLM extraction uses the full requirements.txt locally.

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.29.0
pydantic>=2.6.0
python-dateutil>=2.9.0
pymupdf>=1.24.0
pillow>=10.0.0
